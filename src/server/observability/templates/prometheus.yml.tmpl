global:
  scrape_interval: 15s
  evaluation_interval: 15s

storage:
  tsdb:
    path: {{STORAGE_PATH}}
    retention.time: 7d

scrape_configs:
  - job_name: prometheus
    static_configs:
      - targets: ["localhost:{{PORT}}"]

remote_write: []
